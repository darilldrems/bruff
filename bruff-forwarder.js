/**
 * this is a simple method for 
 * forwarding request from client directly
 * to origin server
 * @param req {Object} this is express request object from the client
 * @param url {String}
 */
module.exports = function (req, url) {
    
}